export interface ITranslateHistory {
  id: number;
  userId: number;
  fromLanguage: string;
  fromText: string;
  toLanguage: string;
  toText: string;
  translateDate: string;
  translateTime: string;
}
